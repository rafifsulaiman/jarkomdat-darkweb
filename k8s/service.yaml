apiVersion: v1
kind: Service
metadata:
  name: insurance-be-service
  namespace: default
spec:
  selector:
    app: insurance-be
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
