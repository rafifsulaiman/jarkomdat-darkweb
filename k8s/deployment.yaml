apiVersion: apps/v1
kind: Deployment
metadata:
  name: darkweb-1
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: darkweb-1
  template:
    metadata:
      labels:
        app: darkweb-1
    spec:
      containers:
        - name: springboot-container
          image: rafifsulaiman/darkweb:REPLACE_ME_TAG
          ports:
            - containerPort: 8080
          env:
            - name: DATABASE_URL_PROD
              valueFrom:
                configMapKeyRef:
                  name: darkweb-1-config
                  key: DATABASE_URL_PROD
            - name: DATABASE_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: darkweb-1-config
                  key: DATABASE_USERNAME
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: darkweb-1-secret
                  key: DATABASE_PASSWORD